type Query {
    coffee(type: CoffeeType): [Coffee]
}

type Coffee {
    title: String
    description: String
    ingredients: [String]
    image: String
    id: ID
}

enum CoffeeType {
    HOT
    ICED
}